import React from "react";
import { render } from "@testing-library/react";
import MainMenu from "./index";

test("Contact Btn opens new tab on click", () => {
  it("10 equals 10", () => {
    expect(10).toEqual(10);
  });
});
